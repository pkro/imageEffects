Adds a shadow to transparent pngs

Usage:
Add to html head:

  `<script src="imageEffects.js"></script>`


Add shadow class to img element:

  `<img src="squid.png" class="shadow " />`
  
  Examples:
  <img src="screenshot.png">
